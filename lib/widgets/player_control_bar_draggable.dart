import 'package:flutter/material.dart';

import 'player_control_bar.dart';

class DraggablePlayerControlBar extends StatelessWidget {
  const DraggablePlayerControlBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlayerControlBar();
  }
}
